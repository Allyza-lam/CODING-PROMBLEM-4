You sent
let favNumber = Math.floor(Math.random() * 22) + 1; 

let guess;
while (guess !== favNumber) {
    guess = parseInt(prompt("Guess the favorite number."));

    if (guess < favNumber) {
        alert("Too low, try again.");
    } else if (guess > favNumber){
        alert("Too high, try again.")
    } else {
        console.log("Correct, you guessed the favorite number.");
        break;
    }
}

<!DOCTYPE html>
<html lang = "en">

<head>
    <meta charset = "UTF-8">
    <meta name = "viewport" content = "width = device-width, initial-scale = 1.0">
    <title>Document</title>

    </head>

    <body>
        <button onClick = "favnum()">Click me</button>

        <script src = "favNumCalcu.js"></script>

    </body>
    </html>
